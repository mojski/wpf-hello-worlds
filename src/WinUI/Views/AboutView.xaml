<syncfusion:RibbonWindow
    x:Class="WinUI.Views.AboutView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
    Title="{DynamicResource ResourceKey=AboutWindowTitle}"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Label
            Grid.Row="0"
            Margin="5"
            HorizontalAlignment="Left"
            Content="{DynamicResource ResourceKey=AboutWindowMainLabel}"
            FontSize="30"
            FontWeight="Bold" />
        <TextBlock
            Grid.Row="1"
            Margin="10"
            Text="{DynamicResource ResourceKey=AboutWindowDescription}"
            TextWrapping="WrapWithOverflow" />
        <StackPanel Grid.Row="2">
            <Label HorizontalAlignment="Left" Content="{DynamicResource ResourceKey=FooterLabel}" />
        </StackPanel>
    </Grid>
</syncfusion:RibbonWindow>
